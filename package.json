{
  "name": "event-scan",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@arianee/arianeejs": "^1.68.0",
    "node-sass": "^4.13.0",
    "react": "^16.12.0",
    "react-dom": "^16.12.0",
    "react-qr-reader": "^2.2.1",
    "react-router-dom": "^5.1.2",
    "react-scripts": "3.2.0",
    "serve": "^11.2.0"
  },
  "scripts": {
    "start": "serve -s build",
    "launch": "HTTPS=true react-scripts start",
    "launch-nossl": " react-scripts start", 
    "start-ssl": "export HTTPS=true&&SSL_CRT_FILE=cert.pem&&SSL_KEY_FILE=key.pem react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  }
}
